<%= form_for(@all_report) do |f| %>
  <% if @all_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@all_report.errors.count, "error") %> prohibited this all_report from being saved:</h2>

      <ul>
      <% @all_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
